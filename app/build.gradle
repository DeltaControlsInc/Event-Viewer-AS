apply plugin: 'com.android.application'

android {
    compileSdkVersion 17
    buildToolsVersion "22.0.1"

    defaultConfig {
        applicationId "com.deltacontrols.eventviewer"
        minSdkVersion 11
        targetSdkVersion 19
        versionCode 1
        versionName "1.05.001"
    }

    signingConfigs {
        release {
            storeFile file("C:\\v0.1\\mobile\\android\\automated\\keystore\\android_keystore")
            storePassword "0ffNRunning"
            keyAlias "eventviewerreleasekey"
            keyPassword "0ffNRunning"
        }
    }
    buildTypes {
        release {
            minifyEnabled false
            proguardFiles getDefaultProguardFile('proguard-android.txt'), 'proguard-rules.pro'
            signingConfig signingConfigs.release
        }
    }
}

repositories {
    flatDir {
        dir 'libs'
    }
}

dependencies {
    compile 'com.android.support:support-v4:18.0.0'
    compile (name:'deltacontrols-eweb-support',ext:'aar')
    compile 'com.google.code.gson:gson:2.2.4'
}
